---
import Layout from "../layouts/Layout.astro";
import { getCollection } from "astro:content";
import PaginationSlider from "../components/PaginationSlider";
import CardProd from "../components/CardProd.astro";
import React from "react";
import ProductSlider from "../components/ProductSlider";


const items = await getCollection("equipos");
const accs= await getCollection("accesorios");
const subcarpetas = Array.from(
    new Set(accs.map((entry) => entry.slug.split('/')[0]))
  );

  console.log(items);
---

<Layout title="teSTPage" theme="white">
  <ProductSlider products={accs} cat="/accesorios/" client:idle/>
</Layout>

<script>
    const fP= ()=>{
        if(window.innerWidth < 1200){
            return 6;
        }
        else if(window.innerWidth < 800){
            return 4;
        }
    }
</script>